version: "3.8"

services:
  nodecast-tv:
    build: .
    container_name: nodecast-tv
    ports:
      - "3000:3000"
    volumes:
      - nodecast_data:/app/data
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000

volumes:
  nodecast_data:
